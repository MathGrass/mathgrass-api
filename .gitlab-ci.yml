image: python:latest

pages:
  stage: deploy
  only:
    changes:
      - docs/**/*
      - mkdocs.yml
  script:
    - pip install mkdocs
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public